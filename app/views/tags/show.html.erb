<h1><%=  @tag.name %></h1>
<br>
<% if @tag.posts.empty?%>
                <h2 class="text-center">There are no post</h2>
                <% else %>
             <%= render @tags%>
             <%end%>
